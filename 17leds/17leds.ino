
void setup() {
  pinMode(13, OUTPUT); 
  pinMode(12, OUTPUT);
  pinMode(14, OUTPUT);
  pinMode(27, OUTPUT);
  pinMode(26, OUTPUT);// Set the LED pin as output
  pinMode(25, OUTPUT);
  pinMode(33, OUTPUT);
  pinMode(32, OUTPUT);
  pinMode(15, OUTPUT);
  pinMode(2, OUTPUT);
  pinMode(4, OUTPUT);
  pinMode(5, OUTPUT);
  pinMode(18, OUTPUT);
  pinMode(19, OUTPUT);
  pinMode(21, OUTPUT);
  pinMode(22, OUTPUT);
  pinMode(23, OUTPUT);
  

}

void loop() {
  digitalWrite(13, HIGH);  // Turn the LED on (HIGH is the voltage level)
  delay(250);                      // Wait for 1 second (2500 milliseconds)
  digitalWrite(13, LOW);   // Turn the LED off (LOW is the voltage level)
  delay(250);
  
  digitalWrite(12, HIGH);  // Turn the LED on (HIGH is the voltage level)
  delay(250);                      // Wait for 1 second (2500 milliseconds)
  digitalWrite(12, LOW);   // Turn the LED off (LOW is the voltage level)
  delay(250); 
  digitalWrite(14, HIGH);  // Turn the LED on (HIGH is the voltage level)
  delay(250);                      // Wait for 1 second (2500 milliseconds)
  digitalWrite(14, LOW);   // Turn the LED off (LOW is the voltage level)
  delay(250); 
  digitalWrite(27, HIGH);  // Turn the LED on (HIGH is the voltage level)
  delay(250);                      // Wait for 1 second (2500 milliseconds)
  digitalWrite(27, LOW);   // Turn the LED off (LOW is the voltage level)
  delay(250); 
  digitalWrite(26, HIGH);  // Turn the LED on (HIGH is the voltage level)
  delay(250);                      // Wait for 1 second (2500 milliseconds)
  digitalWrite(26, LOW);   // Turn the LED off (LOW is the voltage level)
  delay(250); 
  digitalWrite(25, HIGH);  // Turn the LED on (HIGH is the voltage level)
  delay(250);                      // Wait for 1 second (2500 milliseconds)
  digitalWrite(25, LOW);   // Turn the LED off (LOW is the voltage level)
  delay(250); 
  digitalWrite(33, HIGH);  // Turn the LED on (HIGH is the voltage level)
  delay(250);                      // Wait for 1 second (2500 milliseconds)
  digitalWrite(33, LOW);   // Turn the LED off (LOW is the voltage level)
  delay(250); 
  digitalWrite(32, HIGH);  // Turn the LED on (HIGH is the voltage level)
  delay(250);                      // Wait for 1 second (2500 milliseconds)
  digitalWrite(32, LOW);   // Turn the LED off (LOW is the voltage level)
  delay(250); 
  digitalWrite(15, HIGH);  // Turn the LED on (HIGH is the voltage level)
  delay(250);                      // Wait for 1 second (2500 milliseconds)
  digitalWrite(15, LOW);   // Turn the LED off (LOW is the voltage level)
  delay(250);   
  
  digitalWrite(2, HIGH);  // Turn the LED on (HIGH is the voltage level)
  delay(250);                      // Wait for 1 second (2500 milliseconds)
  digitalWrite(2, LOW);   // Turn the LED off (LOW is the voltage level)
  delay(250); 
  
  digitalWrite(4, HIGH);  // Turn the LED on (HIGH is the voltage level)
  delay(250);                      // Wait for 1 second (2500 milliseconds)
  digitalWrite(4, LOW);   // Turn the LED off (LOW is the voltage level)
  delay(250);      
  
  digitalWrite(5, HIGH);  // Turn the LED on (HIGH is the voltage level)
  delay(250);                      // Wait for 1 second (2500 milliseconds)
  digitalWrite(5, LOW);   // Turn the LED off (LOW is the voltage level)
  delay(250); 
  digitalWrite(18, HIGH);  // Turn the LED on (HIGH is the voltage level)
  delay(250);                      // Wait for 1 second (2500 milliseconds)
  digitalWrite(18, LOW);   // Turn the LED off (LOW is the voltage level)
  delay(250); 
  digitalWrite(19, HIGH);  // Turn the LED on (HIGH is the voltage level)
  delay(250);                      // Wait for 1 second (2500 milliseconds)
  digitalWrite(19, LOW);   // Turn the LED off (LOW is the voltage level)
  delay(250); 
  digitalWrite(21, HIGH);  // Turn the LED on (HIGH is the voltage level)
  delay(250);                      // Wait for 1 second (2500 milliseconds)
  digitalWrite(21, LOW);   // Turn the LED off (LOW is the voltage level)
  delay(250); 
  digitalWrite(22, HIGH);  // Turn the LED on (HIGH is the voltage level)
  delay(250);                      // Wait for 1 second (2500 milliseconds)
  digitalWrite(22, LOW);   // Turn the LED off (LOW is the voltage level)
  delay(250); 
  digitalWrite(23, HIGH);  // Turn the LED on (HIGH is the voltage level)
  delay(250);                      // Wait for 1 second (2500 milliseconds)
  digitalWrite(23, LOW);   // Turn the LED off (LOW is the voltage level)
  delay(250);                // Wait for 1 second
}
